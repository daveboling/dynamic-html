<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<link rel="stylesheet" href="/css/styles.css">
	<title>Checkers</title>
</head>
<body>
	<div>
<!--
	for(var i = 0; i < 8; i++){
		//create row

		//if odd, class = odd

		//else class = even

		for(var k = 0; k < 8; k++) {
			//create column

			//if odd, class = colOdd

			//else, class = colEven
		}
	}	
-->
<table>
	<% 	var s = 0;
		var type;
		for(var i = 1; i <= 8; i++) { %>	
		<tr>
		<% for(var k = 1; k <= 8; k++) { 
			s++
			console.log(s);
				if( s == 2 || s == 4 || s == 6 || s == 8 ||
					s == 9 || s == 11 || s == 13 || s == 15 ||
					s == 18 || s == 20 || s == 22 || s == 24) { type = 'red'}
				else if(s == 41 || s == 43 || s == 45 || s == 47 ||
						s == 50 || s == 52 || s == 54 || s == 56 ||
						s == 57 || s == 59 || s == 61 || s == 63){ type = 'black'}
				else { type = 'blank'}
		%>
			 <td><div class="<%= type %>">
			</div></td>
		<% } %>
		</tr>
    <% } %>
</table>
	</div>
</body>
</html>

